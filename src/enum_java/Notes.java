/* Enumerations serve the purpose of representing a group of named constants in a programming language*/

/* The main objective of enums is to allow us to create our own data type*/

/* ABOUT ENUMS
 * 
 * we cannot create an enum inside a method but it can be inside a class or outside the class
 * 
 * to create enum we use enum keyword and not class or interface
 * 
 * according to java naming convention enum should be all capital letters
 * 
 * we can create object of enum
 * 
 */