package java_database_connectivity_jdbc;
/*WE NEED TO FOLLOW THESE 7 STEPS TO CONNECT TO DATABASE
 * import the package ---> java.sql.*;
 * load and register the driver
 *  ---> load for mysql --> com.myql.jdbc.Driver
 *  ---> register ---> class.forName(com.myql.jdbc.Driver)
 * establish the connection  ---> using interface called Connection
 * create the statement
 * ---> Normal statement
 * ---> Prepared statement
 * ---> callable statement
 * execute the query
 * process results
 * close the connection*/
 

/*WHILE DEALING WITH MYSQL
 * username = root
 * passowrd = root
 * 
 *  use <jinaladatabase> then run ili kuselect kuwa unatumia database ipi
 *  select * from <jinalatable> ili kudisplay table yote
 *  select <jinalarow> from <jinalatable> where <jinalaid>=<idunayoitakakudisplya>*/
 
/*THREE TYPES OF DATABASE LANGUAGE
 *DDL ---> creating a table / changing a structure of database
 *DML ---> inserting / updating / deleting a value
 *DQL ---> fetching data
 *TCL ---> grand / removing permission */
 